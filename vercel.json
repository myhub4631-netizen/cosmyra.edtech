{
  "version": 2,
  "buildCommand": "cd flutter_app && flutter build web --release",
  "outputDirectory": "flutter_app/build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
