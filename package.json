{
  "name": "cosmyra-edu-flutter-platform",
  "private": true,
  "scripts": {
    "build": "echo 'Using pre-built public directory for Vercel deployment'",
    "start": "cd flutter_app && flutter run -d chrome"
  }
}
